# ═══════════════════════════════════════════════════════════════════════════════
# BlackBook L1 Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!

# ─────────────────────────────────────────────────────────────────────────────────
# SUPABASE CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ─────────────────────────────────────────────────────────────────────────────────
# SERVER CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────────
PORT=8080
RUST_LOG=info

# ─────────────────────────────────────────────────────────────────────────────────
# DEALER ORACLE ACCOUNT (L2 "House" Account)
# ─────────────────────────────────────────────────────────────────────────────────
# ⚠️ CRITICAL: The Dealer is the L2 oracle that creates/resolves prediction markets
# The private key MUST be kept secret and NEVER committed to version control
#
# Generate a new keypair for production:
#   node -e "const nacl = require('tweetnacl'); const kp = nacl.sign.keyPair(); console.log('Private:', Buffer.from(kp.secretKey.slice(0,32)).toString('hex')); console.log('Public:', Buffer.from(kp.publicKey).toString('hex'));"
#
# Dealer Public Key (for reference - also in unified_auth.rs):
#   07943256765557e704e4945aa4d1d56a1b0aac60bd8cc328faa99572aee5e84a
#
# Dealer Address:
#   L1_F5C46483E8A28394F5E8687DEADF6BD4E924CED3
#
DEALER_PRIVATE_KEY=your-dealer-private-key-hex-here

# ─────────────────────────────────────────────────────────────────────────────────
# SSS RECOVERY ENCRYPTION
# ─────────────────────────────────────────────────────────────────────────────────
# Server-side encryption key for SSS Share 1 (stored in Supabase)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
SERVER_ENCRYPTION_KEY=your-64-char-hex-key-here

# ─────────────────────────────────────────────────────────────────────────────────
# BLOCKCHAIN CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────────
CHAIN_ID_L1=0x01
CHAIN_ID_L2=0x02

# ─────────────────────────────────────────────────────────────────────────────────
# OPTIONAL: EXTERNAL SERVICES
# ─────────────────────────────────────────────────────────────────────────────────
# SENDGRID_API_KEY=your-sendgrid-key-here  # For email recovery shares
# AWS_S3_BUCKET=your-bucket-name           # For backup storage
