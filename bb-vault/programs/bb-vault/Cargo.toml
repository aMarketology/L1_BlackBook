[package]
name = "bb-vault"
version = "0.1.0"
description = "BlackBook USDT Vault — KMS-gated outbound claims with replay protection"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "bb_vault"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30"
anchor-spl = "0.30"
solana-program = "1.18"
